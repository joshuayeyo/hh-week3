import './notifications/createNotificationMessage.spec';
import './notifications/getUpcomingEvents.spec';

describe('notificationUtils Integration Tests', () => {
  it('모든 notificationUtils 테스트 모듈이 정상적으로 로드되었는지 확인', () => {
    expect(true).toBe(true);
  });
});
